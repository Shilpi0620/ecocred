import React from "react";
import ReactDOM from "react-dom/client";

import EcoCredFrontend from "../ecocredfrontend.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EcoCredFrontend />
  </React.StrictMode>
);
